import React from "react";
import "../css/SubHeader.css"; // Import the CSS file

const SubHeader = () => {
  return (
    <div className="sub-header">
      <p className="sub-header-text">
        Anything that can go wrong, will go wrong
      </p>
    </div>
  );
};

export default SubHeader;
